@model IEnumerable<deneme12.Entity.DersProgramiList>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
           
            <th>
                @Html.DisplayNameFor(model => model.DersProgramiListName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
               
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DersProgramiListName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DersProgramiListId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DersProgramiListId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DersProgramiListId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
